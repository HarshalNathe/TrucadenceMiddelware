x-swagger-router-controller: dashboard-controller
get:
  operationId: monthlyRegistrationStatsGet
  description: "Display candidates in that month"
  summary: "Display candidates in that month"
  security:
    - OAuth2: []
  tags:
    - "/dashboard"
  parameters: []
  responses:
    "200":
      description: "Success"
      schema:
        $ref: "#/definitions/Response"
    "403":
      description: "Unable to display"
      schema:
        $ref: "#/definitions/Error"
