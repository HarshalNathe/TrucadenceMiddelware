x-swagger-router-controller: dashboard-controller
get:
  operationId: profileStatsGet
  description: "Display status of form filled"
  summary: "Display status of form filled"
  security:
    - OAuth2: []
  tags:
    - "/dashboard"
  parameters: []
  responses:
    "200":
      description: "Success"
      schema:
        $ref: "#/definitions/Response"
    "403":
      description: "Unable to display"
      schema:
        $ref: "#/definitions/Error"